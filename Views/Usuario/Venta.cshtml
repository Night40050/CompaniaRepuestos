@model CompaniaRepuestos.ViewModels.CrearVentaViewModel

@{
    ViewData["Title"] = "Venta";
}

<h1>Venta</h1>

<h4>Venta</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Venta">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Producto.idProducto" class="control-label"></label>
                <input asp-for="Producto.idProducto" class="form-control" />
                <span asp-validation-for="Producto.idProducto" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Venta.cantidadVendida" class="control-label"></label>
                <input asp-for="Venta.cantidadVendida" class="form-control" />
                <span asp-validation-for="Venta.cantidadVendida" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Venta">Back to List</a>
</div>

